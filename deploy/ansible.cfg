[defaults]
remote_user = root
nocows = 1
interpreter_python = /usr/bin/python

[ssh_connection]
pipelining = True
scp_if_ssh = True
retries = 2

[persistent_connection]
connect_timeout = 5
command_timeout = 60
